# Agent Workflow 仓库规则

## 版本与标签

- 用户要求：后续 AW 更新只要变更版本号，就同步创建并推送对应的 Git tag。
- 以 `package.json` 的版本为准，同步更新 `package-lock.json` 和相关版本说明；标签使用 `vX.Y.Z`，例如 `v0.4.4`。
- 完成相应验证并提交版本变更后，在该发布提交上创建 annotated tag；推送发布提交及该标签到 `origin`，核对远端标签指向正确提交后再报告发布完成。
- 已存在的同名标签应先核对目标，不覆盖或移动已发布标签。未变更版本号的普通提交无需新标签。
